diff --git a/play.pokemonshowdown.com/index-new.html b/play.pokemonshowdown.com/index-new.html
index 4dc1c78..a358960 100644
--- a/play.pokemonshowdown.com/index-new.html
+++ b/play.pokemonshowdown.com/index-new.html
@@ -44,7 +44,7 @@ https://psim.us/dev
 		<div class="ps-room scrollable" id="room-">
 			<div class="mainmenu-mini-windows">
 				<div class="mini-window" data-roomid="news">
-					<h3 class="mini-window-header"><button class="closebutton" tabindex="-1" aria-label="Close"><i class="fa fa-times-circle"></i></button>News</h3>
+					<h3 class="mini-window-header"><button class="closebutton" tabindex="-1" aria-label="关闭"><i class="fa fa-times-circle"></i></button>新闻</h3>
 					<div id="room-news" class="mini-window-contents ps-room-light scrollable" style="height: auto;" data-newsid="<!-- newsid -->">
 						<div class="readable-bg">
 							<!-- build-tools/news-embed.php -->
@@ -53,19 +53,20 @@ https://psim.us/dev
 				</div>
 			</div>
 			<div class="mainmenu">
-				<div id="loading-message" class="mainmessage">Loading... <noscript>FAILED<br /><br />Pok&eacute;mon Showdown requires JavaScript.</noscript></div>
+				<div id="loading-message" class="mainmessage">加载中…… <noscript>加载失败<br /><br />Pok&eacute;mon Showdown 需要启用 JavaScript。</noscript></div>
 			</div>
 			<div class="mainmenufooter">
 				<div class="bgcredit"></div>
 				<small>
-					<a href="//dex.pokemonshowdown.com/" target="_blank">Pok&eacute;dex</a> | <a href="//replay.pokemonshowdown.com/" target="_blank">Replays</a> | <a href="http://smogon.com/forums/" target="_blank">Forum</a><br />
-					<a href="//pokemonshowdown.com/rules" target="_blank">Rules</a> | <a href="//pokemonshowdown.com/credits" target="_blank">Credits</a> | <a href="//pokemonshowdown.com/privacy" target="_blank">Privacy</a>
+					<a href="//dex.pokemonshowdown.com/" target="_blank">图鉴</a> | <a href="//replay.pokemonshowdown.com/" target="_blank">回放</a> | <a href="http://smogon.com/forums/" target="_blank">论坛</a><br />
+					<a href="//pokemonshowdown.com/rules" target="_blank">规则</a> | <a href="//pokemonshowdown.com/credits" target="_blank">贡献者</a> | <a href="//pokemonshowdown.com/privacy" target="_blank">隐私</a>
 				</small>
 			</div>
 		</div>
 	</div>
 	<script nomodule defer src="/js/lib/ps-polyfill.js"></script>
 	<script defer src="/config/config.js?"></script>
+	<script defer src="/js/localization-zh-hans.js?"></script>
 	<script defer src="/js/client-core.js?"></script>
 	<!-- At this point, background and dark mode are loaded -->
 	<!-- config/head-custom.html -->
@@ -91,6 +92,7 @@ https://psim.us/dev
 	<script defer src="/js/client-connection.js?"></script>
 
 	<script defer src="/js/lib/preact.min.js?"></script>
+	<script defer src="/js/localization-preact-init.js?"></script>
 	<script defer src="/js/panels.js?"></script>
 	<script defer src="/js/panel-mainmenu.js?"></script>
 	<script defer src="/js/panel-rooms.js?"></script>
diff --git a/play.pokemonshowdown.com/src/battle-animations.ts b/play.pokemonshowdown.com/src/battle-animations.ts
index 9ed8a19..9c438e4 100644
--- a/play.pokemonshowdown.com/src/battle-animations.ts
+++ b/play.pokemonshowdown.com/src/battle-animations.ts
@@ -1077,7 +1077,8 @@ export class BattleScene implements BattleSceneStub {
 			this.$turn.html('');
 			return;
 		}
-		this.$turn.html(`<div class="turn has-tooltip" data-tooltip="field" data-ownheight="1">Turn ${this.battle.turn}</div>`);
+		const turnText = window.PSLocalizer?.battle(`Turn ${this.battle.turn}`) || `Turn ${this.battle.turn}`;
+		this.$turn.html(`<div class="turn has-tooltip" data-tooltip="field" data-ownheight="1">${BattleLog.escapeHTML(turnText)}</div>`);
 	}
 	incrementTurn() {
 		if (!this.animating) return;
@@ -1085,7 +1086,8 @@ export class BattleScene implements BattleSceneStub {
 		const turn = this.battle.turn;
 		if (turn <= 0) return;
 		const $prevTurn = this.$turn.children();
-		const $newTurn = $(`<div class="turn has-tooltip" data-tooltip="field" data-ownheight="1">Turn ${turn}</div>`);
+		const turnText = window.PSLocalizer?.battle(`Turn ${turn}`) || `Turn ${turn}`;
+		const $newTurn = $(`<div class="turn has-tooltip" data-tooltip="field" data-ownheight="1">${BattleLog.escapeHTML(turnText)}</div>`);
 		$newTurn.css({
 			opacity: 0,
 			left: 160,
diff --git a/play.pokemonshowdown.com/src/battle-dex-search.ts b/play.pokemonshowdown.com/src/battle-dex-search.ts
index b17c547..26ffd4b 100644
--- a/play.pokemonshowdown.com/src/battle-dex-search.ts
+++ b/play.pokemonshowdown.com/src/battle-dex-search.ts
@@ -109,6 +109,28 @@ export class DexSearch {
 	}
 
 	find(query: string) {
+		const localizedQuery = query.trim();
+		const localizedRows = window.PSLocalizer?.search(
+			localizedQuery,
+			this.typedSearch?.searchType || ''
+		) as SearchRow[] | null;
+		if (localizedRows?.length) {
+			const queryKey = `zh:${localizedQuery}`;
+			if (this.query === queryKey && this.results) return false;
+			this.query = queryKey;
+			if (this.typedSearch) {
+				const allowed: { [key: string]: 1 } = {};
+				for (const row of this.typedSearch.getResults(this.filters, this.sortCol, this.reverseSort)) {
+					allowed[`${row[0]}:${row[1]}`] = 1;
+				}
+				this.results = localizedRows.filter(row => allowed[`${row[0]}:${row[1]}`]);
+			} else {
+				this.results = localizedRows;
+			}
+			this.selection = this.getFirstResultIndex();
+			this.exactMatch = this.results.length === 1;
+			return true;
+		}
 		query = toID(query);
 		if (this.query === query && this.results) {
 			return false;
diff --git a/play.pokemonshowdown.com/src/battle-log.ts b/play.pokemonshowdown.com/src/battle-log.ts
index 77cf1d8..d4c4b5b 100644
--- a/play.pokemonshowdown.com/src/battle-log.ts
+++ b/play.pokemonshowdown.com/src/battle-log.ts
@@ -979,10 +979,14 @@ export class BattleLog {
 		];
 	}
 	message(message: string, sceneMessage = message) {
+		sceneMessage = window.PSLocalizer?.battle(sceneMessage) || sceneMessage;
 		this.scene?.message(sceneMessage);
 		this.addDiv('battle-history', message);
 	}
 	addNode(node: HTMLElement, preempt?: boolean) {
+		if (!node.classList.contains('chat') || node.classList.contains('message-error')) {
+			window.PSLocalizer?.element(node, true);
+		}
 		(preempt ? this.preemptElem : this.innerElem).appendChild(node);
 		if (this.atBottom) {
 			this.elem.scrollTop = this.elem.scrollHeight;
@@ -1003,6 +1007,9 @@ export class BattleLog {
 		const el = document.createElement('div');
 		el.className = className;
 		el.innerHTML = innerHTML;
+		if (!el.classList.contains('chat') || el.classList.contains('message-error')) {
+			window.PSLocalizer?.element(el, true);
+		}
 		if (this.innerElem.childNodes.length) {
 			this.innerElem.insertBefore(el, this.innerElem.childNodes[0]);
 		} else {
diff --git a/play.pokemonshowdown.com/src/battle-searchresults.tsx b/play.pokemonshowdown.com/src/battle-searchresults.tsx
index af920c9..f1bdabe 100644
--- a/play.pokemonshowdown.com/src/battle-searchresults.tsx
+++ b/play.pokemonshowdown.com/src/battle-searchresults.tsx
@@ -27,6 +27,10 @@ function escapeCSSString(text: string) {
 	return text.replace(/\\/g, '\\\\').replace(/"/g, '\\"').replace(/\n/g, '\\a ').replace(/\r/g, '\\d ');
 }
 
+function localize(text: string) {
+	return window.PSLocalizer?.exact(text) || text;
+}
+
 export class PSSearchResults extends preact.Component<{
 	search: DexSearch, class?: string, style?: string | null,
 	prepend?: preact.ComponentChildren, children?: preact.ComponentChildren,
@@ -51,17 +55,17 @@ export class PSSearchResults extends preact.Component<{
 		return [
 			`<li class="result" value="${index}"><div class="sortrow">`,
 			`<button class="sortcol numsortcol${!sortCol ? ' cur' : ''}">`,
-			`${!sortCol ? 'Sort: ' : escapeHTML(search.firstPokemonColumn)}</button>`,
-			`<button class="sortcol pnamesortcol${sortCol === 'name' ? ' cur' : ''}" data-sort="name">Name</button>`,
-			`<button class="sortcol typesortcol${sortCol === 'type' ? ' cur' : ''}" data-sort="type">Types</button>`,
-			`<button class="sortcol abilitysortcol${sortCol === 'ability' ? ' cur' : ''}" data-sort="ability">Abilities</button>`,
+			`${escapeHTML(localize(!sortCol ? 'Sort: ' : search.firstPokemonColumn))}</button>`,
+			`<button class="sortcol pnamesortcol${sortCol === 'name' ? ' cur' : ''}" data-sort="name">${escapeHTML(localize('Name'))}</button>`,
+			`<button class="sortcol typesortcol${sortCol === 'type' ? ' cur' : ''}" data-sort="type">${escapeHTML(localize('Types'))}</button>`,
+			`<button class="sortcol abilitysortcol${sortCol === 'ability' ? ' cur' : ''}" data-sort="ability">${escapeHTML(localize('Abilities'))}</button>`,
 			`<button class="sortcol statsortcol${sortCol === 'hp' ? ' cur' : ''}" data-sort="hp">HP</button>`,
-			`<button class="sortcol statsortcol${sortCol === 'atk' ? ' cur' : ''}" data-sort="atk">Atk</button>`,
-			`<button class="sortcol statsortcol${sortCol === 'def' ? ' cur' : ''}" data-sort="def">Def</button>`,
-			`<button class="sortcol statsortcol${sortCol === 'spa' ? ' cur' : ''}" data-sort="spa">SpA</button>`,
-			`<button class="sortcol statsortcol${sortCol === 'spd' ? ' cur' : ''}" data-sort="spd">SpD</button>`,
-			`<button class="sortcol statsortcol${sortCol === 'spe' ? ' cur' : ''}" data-sort="spe">Spe</button>`,
-			`<button class="sortcol statsortcol${sortCol === 'bst' ? ' cur' : ''}" data-sort="bst">BST</button>`,
+			`<button class="sortcol statsortcol${sortCol === 'atk' ? ' cur' : ''}" data-sort="atk">${escapeHTML(localize('Atk'))}</button>`,
+			`<button class="sortcol statsortcol${sortCol === 'def' ? ' cur' : ''}" data-sort="def">${escapeHTML(localize('Def'))}</button>`,
+			`<button class="sortcol statsortcol${sortCol === 'spa' ? ' cur' : ''}" data-sort="spa">${escapeHTML(localize('SpA'))}</button>`,
+			`<button class="sortcol statsortcol${sortCol === 'spd' ? ' cur' : ''}" data-sort="spd">${escapeHTML(localize('SpD'))}</button>`,
+			`<button class="sortcol statsortcol${sortCol === 'spe' ? ' cur' : ''}" data-sort="spe">${escapeHTML(localize('Spe'))}</button>`,
+			`<button class="sortcol statsortcol${sortCol === 'bst' ? ' cur' : ''}" data-sort="bst">${escapeHTML(localize('BST'))}</button>`,
 			`</div></li>`,
 		].join('');
 	}
@@ -69,11 +73,11 @@ export class PSSearchResults extends preact.Component<{
 	renderMoveSortRowHTML(index: number) {
 		const sortCol = this.props.search.sortCol;
 		return `<li class="result" value="${index}"><div class="sortrow">` +
-			`<button class="sortcol movenamesortcol${sortCol === 'name' ? ' cur' : ''}" data-sort="name">Name</button>` +
-			`<button class="sortcol movetypesortcol${sortCol === 'type' ? ' cur' : ''}" data-sort="type">Type</button>` +
-			`<button class="sortcol movetypesortcol${sortCol === 'category' ? ' cur' : ''}" data-sort="category">Cat</button>` +
-			`<button class="sortcol powersortcol${sortCol === 'power' ? ' cur' : ''}" data-sort="power">Pow</button>` +
-			`<button class="sortcol accuracysortcol${sortCol === 'accuracy' ? ' cur' : ''}" data-sort="accuracy">Acc</button>` +
+			`<button class="sortcol movenamesortcol${sortCol === 'name' ? ' cur' : ''}" data-sort="name">${escapeHTML(localize('Name'))}</button>` +
+			`<button class="sortcol movetypesortcol${sortCol === 'type' ? ' cur' : ''}" data-sort="type">${escapeHTML(localize('Type'))}</button>` +
+			`<button class="sortcol movetypesortcol${sortCol === 'category' ? ' cur' : ''}" data-sort="category">${escapeHTML(localize('Cat'))}</button>` +
+			`<button class="sortcol powersortcol${sortCol === 'power' ? ' cur' : ''}" data-sort="power">${escapeHTML(localize('Pow'))}</button>` +
+			`<button class="sortcol accuracysortcol${sortCol === 'accuracy' ? ' cur' : ''}" data-sort="accuracy">${escapeHTML(localize('Acc'))}</button>` +
 			`<button class="sortcol ppsortcol${sortCol === 'pp' ? ' cur' : ''}" data-sort="pp">PP</button>` +
 			`</div></li>`;
 	}
@@ -81,9 +85,9 @@ export class PSSearchResults extends preact.Component<{
 	renderPokemonRowHTML(index: number, id: ID, matchStart: number, matchEnd: number, errorMessage?: string) {
 		const search = this.props.search;
 		const pokemon = search.dex.species.get(id);
-		if (!pokemon) return `<li class="result" value="${index}">Unrecognized pokemon</li>`;
+		if (!pokemon) return `<li class="result" value="${index}">${escapeHTML(localize('Unrecognized pokemon'))}</li>`;
 
-		const tagStart = (pokemon.forme ? pokemon.name.length - pokemon.forme.length - 1 : 0);
+		const displayName = window.PSLocalizer?.name('species', id, pokemon.name) || pokemon.name;
 		const stats = pokemon.baseStats;
 		let bst = 0;
 		for (const stat of Object.values(stats)) bst += stat;
@@ -94,7 +98,7 @@ export class PSSearchResults extends preact.Component<{
 			`data-entry="pokemon|${escapeHTML(pokemon.name)}">` +
 			`<span class="col numcol">${escapeHTML(search.getTier(pokemon))}</span>` +
 			`<span class="col iconcol"><span class="pixelated" style="${escapeHTML(Dex.getPokemonIcon(pokemon.id))}"></span></span>` +
-			`<span class="col pokemonnamecol">${this.renderNameHTML(pokemon.name, matchStart, matchEnd, tagStart)}</span>`;
+			`<span class="col pokemonnamecol">${this.renderNameHTML(displayName, 0, 0)}</span>`;
 		if (errorMessage) return `${buf}${errorMessage}</a></li>`;
 
 		buf += `<span class="col typecol">${pokemon.types.map(type =>
@@ -103,16 +107,16 @@ export class PSSearchResults extends preact.Component<{
 
 		if (search.dex.gen >= 3) {
 			buf += pokemon.abilities['1'] ?
-				`<span class="col twoabilitycol">${escapeHTML(pokemon.abilities['0'])}<br />${escapeHTML(pokemon.abilities['1'])}</span>` :
-				`<span class="col abilitycol">${escapeHTML(pokemon.abilities['0'])}</span>`;
+				`<span class="col twoabilitycol">${escapeHTML(window.PSLocalizer?.name('abilities', toID(pokemon.abilities['0']), pokemon.abilities['0']) || pokemon.abilities['0'])}<br />${escapeHTML(window.PSLocalizer?.name('abilities', toID(pokemon.abilities['1']), pokemon.abilities['1']) || pokemon.abilities['1'])}</span>` :
+				`<span class="col abilitycol">${escapeHTML(window.PSLocalizer?.name('abilities', toID(pokemon.abilities['0']), pokemon.abilities['0']) || pokemon.abilities['0'])}</span>`;
 		}
 		if (search.dex.gen >= 5) {
 			if (pokemon.abilities['S']) {
 				buf += `<span class="col twoabilitycol${pokemon.unreleasedHidden ? ' unreleasedhacol' : ''}">` +
-					`${escapeHTML(pokemon.abilities['H'] || '')}<br />${escapeHTML(pokemon.abilities['S'])}</span>`;
+					`${escapeHTML(window.PSLocalizer?.name('abilities', toID(pokemon.abilities['H'] || ''), pokemon.abilities['H'] || '') || pokemon.abilities['H'] || '')}<br />${escapeHTML(window.PSLocalizer?.name('abilities', toID(pokemon.abilities['S']), pokemon.abilities['S']) || pokemon.abilities['S'])}</span>`;
 			} else if (pokemon.abilities['H']) {
 				buf += `<span class="col abilitycol${pokemon.unreleasedHidden ? ' unreleasedhacol' : ''}">` +
-					`${escapeHTML(pokemon.abilities['H'])}</span>`;
+					`${escapeHTML(window.PSLocalizer?.name('abilities', toID(pokemon.abilities['H']), pokemon.abilities['H']) || pokemon.abilities['H'])}</span>`;
 			} else {
 				buf += `<span class="col abilitycol"></span>`;
 			}
@@ -131,7 +135,7 @@ export class PSSearchResults extends preact.Component<{
 	}
 
 	renderNameHTML(name: string, matchStart: number, matchEnd: number, tagStart?: number) {
-		if (name === 'No Ability') return `<i>(no ability)</i>`;
+		if (name === 'No Ability') return `<i>${escapeHTML(localize('(no ability)'))}</i>`;
 
 		if (!matchEnd) {
 			if (!tagStart) return escapeHTML(name);
@@ -155,27 +159,31 @@ export class PSSearchResults extends preact.Component<{
 	renderItemRowHTML(index: number, id: ID, matchStart: number, matchEnd: number, errorMessage?: string) {
 		const search = this.props.search;
 		const item = search.dex.items.get(id);
-		if (!item) return `<li class="result" value="${index}">Unrecognized item</li>`;
+		if (!item) return `<li class="result" value="${index}">${escapeHTML(localize('Unrecognized item'))}</li>`;
 
+		const displayName = window.PSLocalizer?.name('items', id, item.name) || item.name;
+		const description = window.PSLocalizer?.description(item.shortDesc) || item.shortDesc;
 		return `<li class="result" value="${index}"><a href="${this.URL_ROOT}items/${id}" ` +
 			`class="${id === this.itemId ? 'cur' : ''}" data-target="push" data-entry="item|${escapeHTML(item.name)}">` +
 			`<span class="col itemiconcol"><span class="pixelated" style="${escapeHTML(Dex.getItemIcon(item))}"></span></span>` +
-			`<span class="col namecol">${id ? this.renderNameHTML(item.name, matchStart, matchEnd) : '<i>(no item)</i>'}</span>` +
+			`<span class="col namecol">${id ? this.renderNameHTML(displayName, 0, 0) : `<i>${escapeHTML(localize('(no item)'))}</i>`}</span>` +
 			(id ? (errorMessage || '') : '') +
-			(!errorMessage ? `<span class="col itemdesccol">${escapeHTML(item.shortDesc)}</span>` : '') +
+			(!errorMessage ? `<span class="col itemdesccol">${escapeHTML(description)}</span>` : '') +
 			`</a></li>`;
 	}
 
 	renderAbilityRowHTML(index: number, id: ID, matchStart: number, matchEnd: number, errorMessage?: string) {
 		const search = this.props.search;
 		const ability = search.dex.abilities.get(id);
-		if (!ability) return `<li class="result" value="${index}">Unrecognized ability</li>`;
+		if (!ability) return `<li class="result" value="${index}">${escapeHTML(localize('Unrecognized ability'))}</li>`;
 
+		const displayName = window.PSLocalizer?.name('abilities', id, ability.name) || ability.name;
+		const description = window.PSLocalizer?.description(ability.shortDesc) || ability.shortDesc;
 		return `<li class="result" value="${index}"><a href="${this.URL_ROOT}abilities/${id}" ` +
 			`class="${id === this.abilityId ? 'cur' : ''}" data-target="push" data-entry="ability|${escapeHTML(ability.name)}">` +
-			`<span class="col namecol">${id ? this.renderNameHTML(ability.name, matchStart, matchEnd) : '<i>(no ability)</i>'}</span>` +
+			`<span class="col namecol">${id ? this.renderNameHTML(displayName, 0, 0) : `<i>${escapeHTML(localize('(no ability)'))}</i>`}</span>` +
 			(errorMessage || '') +
-			(!errorMessage ? `<span class="col abilitydesccol">${escapeHTML(ability.shortDesc)}</span>` : '') +
+			(!errorMessage ? `<span class="col abilitydesccol">${escapeHTML(description)}</span>` : '') +
 			`</a></li>`;
 	}
 
@@ -192,13 +200,14 @@ export class PSSearchResults extends preact.Component<{
 
 		const search = this.props.search;
 		const move = search.dex.moves.get(id);
-		if (!move) return `<li class="result" value="${index}">Unrecognized move</li>`;
+		if (!move) return `<li class="result" value="${index}">${escapeHTML(localize('Unrecognized move'))}</li>`;
 		const entry = slot ? `move|${move.name}|${slot}` : `move|${move.name}`;
-		const tagStart = (move.name.startsWith('Hidden Power') ? 12 : 0);
+		const displayName = window.PSLocalizer?.name('moves', id, move.name) || move.name;
+		const description = window.PSLocalizer?.description(move.shortDesc) || move.shortDesc;
 
 		let buf = `<li class="result" value="${index}"><a href="${this.URL_ROOT}moves/${id}" ` +
 			`class="${this.moveIds.includes(id) ? 'cur' : ''}" data-target="push" data-entry="${escapeHTML(entry)}">` +
-			`<span class="col movenamecol">${this.renderNameHTML(move.name, matchStart, matchEnd, tagStart)}</span>`;
+			`<span class="col movenamecol">${this.renderNameHTML(displayName, 0, 0)}</span>`;
 		if (errorMessage) return `${buf}${errorMessage}</a></li>`;
 
 		let pp = (move.pp === 1 || move.noPPBoosts ? move.pp : move.pp * 8 / 5);
@@ -213,20 +222,21 @@ export class PSSearchResults extends preact.Component<{
 			`<img src="${Dex.resourcePrefix}sprites/categories/${escapeHTML(move.category)}.png" ` +
 			`alt="${escapeHTML(move.category)}" height="14" width="32" class="pixelated" />` +
 			`</span>` +
-			`<span class="col labelcol">${move.category !== 'Status' ? `<em>Power</em><br />${move.basePower || '&mdash;'}` : ''}</span>` +
-			`<span class="col widelabelcol"><em>Accuracy</em><br />` +
+			`<span class="col labelcol">${move.category !== 'Status' ? `<em>${escapeHTML(localize('Power'))}</em><br />${move.basePower || '&mdash;'}` : ''}</span>` +
+			`<span class="col widelabelcol"><em>${escapeHTML(localize('Accuracy'))}</em><br />` +
 			`${move.accuracy && move.accuracy !== true ? `${move.accuracy}%` : '&mdash;'}</span>` +
 			`<span class="col pplabelcol"><em>PP</em><br />${pp}</span>` +
-			`<span class="col movedesccol">${escapeHTML(move.shortDesc)}</span></a></li>`;
+			`<span class="col movedesccol">${escapeHTML(description)}</span></a></li>`;
 		return buf;
 	}
 
 	renderTypeRowHTML(index: number, id: ID, matchStart: number, matchEnd: number, errorMessage?: string) {
 		const name = id.charAt(0).toUpperCase() + id.slice(1);
+		const displayName = localize(name);
 
 		return `<li class="result" value="${index}"><a href="${this.URL_ROOT}types/${id}" ` +
 			`data-target="push" data-entry="type|${escapeHTML(name)}">` +
-			`<span class="col namecol">${this.renderNameHTML(name, matchStart, matchEnd)}</span>` +
+			`<span class="col namecol">${this.renderNameHTML(displayName, 0, 0)}</span>` +
 			`<span class="col typecol"><img src="${Dex.resourcePrefix}sprites/types/${encodeURIComponent(name)}.png" ` +
 			`alt="${escapeHTML(name)}" height="14" width="32" class="pixelated" /></span>` +
 			(errorMessage || '') +
@@ -235,10 +245,11 @@ export class PSSearchResults extends preact.Component<{
 
 	renderCategoryRowHTML(index: number, id: ID, matchStart: number, matchEnd: number, errorMessage?: string) {
 		const name = id.charAt(0).toUpperCase() + id.slice(1);
+		const displayName = localize(name);
 
 		return `<li class="result" value="${index}"><a href="${this.URL_ROOT}categories/${id}" ` +
 			`data-target="push" data-entry="category|${escapeHTML(name)}">` +
-			`<span class="col namecol">${this.renderNameHTML(name, matchStart, matchEnd)}</span>` +
+			`<span class="col namecol">${this.renderNameHTML(displayName, 0, 0)}</span>` +
 			`<span class="col typecol"><img src="${Dex.resourcePrefix}sprites/categories/${escapeHTML(name)}.png" ` +
 			`alt="${escapeHTML(name)}" height="14" width="32" class="pixelated" /></span>` +
 			(errorMessage || '') +
@@ -252,7 +263,7 @@ export class PSSearchResults extends preact.Component<{
 		return `<li class="result" value="${index}"><a href="${this.URL_ROOT}articles/${id}" ` +
 			`data-target="push" data-entry="article|${escapeHTML(name)}">` +
 			`<span class="col namecol">${this.renderNameHTML(name, matchStart, matchEnd)}</span>` +
-			`<span class="col movedesccol">${isSearchType ? "(search type)" : "(article)"}</span>` +
+			`<span class="col movedesccol">${escapeHTML(localize(isSearchType ? "(search type)" : "(article)"))}</span>` +
 			(errorMessage || '') +
 			`</a></li>`;
 	}
@@ -273,7 +284,7 @@ export class PSSearchResults extends preact.Component<{
 		return `<li class="result" value="${index}"><a href="${this.URL_ROOT}egggroups/${id}" ` +
 			`data-target="push" data-entry="egggroup|${escapeHTML(name)}">` +
 			`<span class="col namecol">${this.renderNameHTML(name, matchStart, matchEnd)}</span>` +
-			`<span class="col movedesccol">(egg group)</span>` +
+			`<span class="col movedesccol">${escapeHTML(localize('(egg group)'))}</span>` +
 			(errorMessage || '') +
 			`</a></li>`;
 	}
@@ -290,7 +301,7 @@ export class PSSearchResults extends preact.Component<{
 		return `<li class="result" value="${index}"><a href="${this.URL_ROOT}tiers/${id}" ` +
 			`data-target="push" data-entry="tier|${escapeHTML(name)}">` +
 			`<span class="col namecol">${this.renderNameHTML(name, matchStart, matchEnd)}</span>` +
-			`<span class="col movedesccol">(tier)</span>` +
+			`<span class="col movedesccol">${escapeHTML(localize('(tier)'))}</span>` +
 			(errorMessage || '') +
 			`</a></li>`;
 	}
@@ -344,7 +355,7 @@ export class PSSearchResults extends preact.Component<{
 		case 'article':
 			return this.renderArticleRowHTML(index, id, matchStart, matchEnd, errorMessage);
 		}
-		return `<li>Error: not found</li>`;
+		return `<li>${escapeHTML(localize('Error: not found'))}</li>`;
 	}
 
 	static renderFilters(search: DexSearch, showHints?: boolean) {
@@ -372,7 +383,7 @@ export class PSSearchResults extends preact.Component<{
 	}
 
 	renderPagerHTML(direction: -1 | 1) {
-		const label = direction < 0 ? 'Show previous search results' : 'Show next search results';
+		const label = localize(direction < 0 ? 'Show previous search results' : 'Show next search results');
 		return `<li class="result resultpage"><button class="button" data-page="${direction}">${label}</button></li>`;
 	}
 
diff --git a/play.pokemonshowdown.com/src/battle-tooltips.ts b/play.pokemonshowdown.com/src/battle-tooltips.ts
index d2d0ebe..2ce5aa3 100644
--- a/play.pokemonshowdown.com/src/battle-tooltips.ts
+++ b/play.pokemonshowdown.com/src/battle-tooltips.ts
@@ -152,6 +152,9 @@ export class BattleTooltips {
 	constructor(battle: Battle) {
 		this.battle = battle;
 	}
+	moveDisplayName(move: Dex.Move) {
+		return window.PSLocalizer?.name('moves', move.id, move.name) || move.name;
+	}
 
 	// tooltips
 	// Touch delay, pressing finger more than that time will cause the tooltip to open.
@@ -464,6 +467,7 @@ export class BattleTooltips {
 		innerHTML = `<div class="tooltipinner"><div class="tooltip tooltip-${type!}">${innerHTML}</div></div>`;
 		$wrapper.html(innerHTML).appendTo(document.body);
 		BattleTooltips.elem = $wrapper.find('.tooltip')[0] as HTMLDivElement;
+		window.PSLocalizer?.element(BattleTooltips.elem);
 		BattleTooltips.isLocked = false;
 
 		let height = $(BattleTooltips.elem).outerHeight()!;
@@ -664,7 +668,7 @@ export class BattleTooltips {
 			});
 		}
 
-		text += `<h2>${move.name}<br />`;
+		text += `<h2>${this.moveDisplayName(move)}<br />`;
 
 		text += Dex.getTypeIcon(moveType);
 		text += ` ${Dex.getCategoryIcon(category)}</h2>`;
@@ -1021,7 +1025,7 @@ export class BattleTooltips {
 			const battlePokemon = clientPokemon || this.battle.findCorrespondingPokemon(pokemon);
 			for (const moveid of serverPokemon.moves) {
 				const move = this.battle.dex.moves.get(moveid);
-				let moveName = `&#8226; ${move.name}`;
+				let moveName = `&#8226; ${this.moveDisplayName(move)}`;
 				if (battlePokemon?.moveTrack) {
 					for (const row of battlePokemon.moveTrack) {
 						if (moveName === row[0]) {
@@ -1038,7 +1042,7 @@ export class BattleTooltips {
 			text += `<p class="tooltip-section">`;
 			for (const [moveName] of clientPokemon.moveTrack) {
 				const move = this.battle.dex.moves.get(moveName);
-				text += `&#8226; ${move.name}<br />`;
+				text += `&#8226; ${this.moveDisplayName(move)}<br />`;
 			}
 			text += `</p>`;
 		} else if (!this.battle.hardcoreMode && clientPokemon?.moveTrack.length) {
@@ -1566,17 +1570,18 @@ export class BattleTooltips {
 			}
 		}
 		const bullet = moveName.startsWith('*') || move.isZ ? '<span style="color:#888">&#8226;</span>' : '&#8226;';
+		const displayName = this.moveDisplayName(move);
 		if (ppUsed === Infinity) {
-			return `${bullet} ${move.name} <small>(0/${maxpp})</small>`;
+			return `${bullet} ${displayName} <small>(0/${maxpp})</small>`;
 		}
 		if (ppUsed || moveName.startsWith('*')) {
 			if (typeof ppUsed === 'number') {
-				return `${bullet} ${move.name} <small>(${maxpp - ppUsed}/${maxpp})</small>`;
+				return `${bullet} ${displayName} <small>(${maxpp - ppUsed}/${maxpp})</small>`;
 			} else {
-				return `${bullet} ${move.name} <small>(${maxpp - ppUsed[0]}/${maxpp} to ${maxpp - ppUsed[1]}/${maxpp})</small>`;
+				return `${bullet} ${displayName} <small>(${maxpp - ppUsed[0]}/${maxpp} to ${maxpp - ppUsed[1]}/${maxpp})</small>`;
 			}
 		}
-		return `${bullet} ${move.name} ${showKnown ? ' <small>(revealed)</small>' : ''}`;
+		return `${bullet} ${displayName} ${showKnown ? ' <small>(revealed)</small>' : ''}`;
 	}
 
 	ppUsed(move: Dex.Move, pokemon: Pokemon) {
diff --git a/play.pokemonshowdown.com/src/client-connection.ts b/play.pokemonshowdown.com/src/client-connection.ts
index 246cd77..83beb40 100644
--- a/play.pokemonshowdown.com/src/client-connection.ts
+++ b/play.pokemonshowdown.com/src/client-connection.ts
@@ -448,6 +448,9 @@ PSConnection.connect();
 
 export const PSLoginServer = new class {
 	rawQuery(act: string, data: PostData): Promise<string | null> {
+		// A private local-auth server has no accounts service. Never let a missed
+		// call fall through to /~~showdown/action.php and wait on the public site.
+		if (Config.localAuth) return Promise.resolve(null);
 		// commenting out because for some reason this is working in Chrome????
 		// if (location.protocol === 'file:') {
 		// 	alert("Sorry, login server queries don't work in the testclient. To log in, see README.md to set up testclient-key.js");
diff --git a/play.pokemonshowdown.com/src/client-main.ts b/play.pokemonshowdown.com/src/client-main.ts
index 6e1feb0..0cac2d5 100644
--- a/play.pokemonshowdown.com/src/client-main.ts
+++ b/play.pokemonshowdown.com/src/client-main.ts
@@ -44,6 +44,8 @@ export interface ServerInfo {
 export interface PSConfig {
 	server: ServerInfo;
 	defaultserver: ServerInfo;
+	/** Allow passwordless nicknames on a private server with noguestsecurity enabled. */
+	localAuth?: boolean;
 	includes?: {
 		mainmenuHTML?: string,
 		roomlistTopHTML?: string,
@@ -533,6 +535,7 @@ class PSTeams extends PSStreamModel<'team' | 'format'> {
 		};
 	}
 	loadRemoteTeams() {
+		if (Config.localAuth) return;
 		PSLoginServer.query('getteams').then(data => {
 			if (!data) return;
 			if (data.actionerror) {
@@ -611,6 +614,7 @@ class PSTeams extends PSStreamModel<'team' | 'format'> {
 	loadTeam(team: Team | undefined | null): Promise<void>;
 	loadTeam(team: Team | undefined | null, ifNeeded?: boolean): void | Promise<void> {
 		if (!team?.uploaded || team.uploadedPackedTeam) return ifNeeded ? undefined : Promise.resolve();
+		if (Config.localAuth) return ifNeeded ? undefined : Promise.resolve();
 		if (team.uploaded.notLoaded && team.uploaded.notLoaded !== true) return team.uploaded.notLoaded;
 
 		const notLoaded = team.uploaded.notLoaded;
@@ -726,6 +730,18 @@ class PSUser extends PSStreamModel<PSLoginState | null> {
 			this.update({ success: true });
 			return;
 		}
+		if (Config.localAuth) {
+			// Private/local servers can accept an empty assertion when
+			// `noguestsecurity` is enabled. This avoids depending on the public
+			// Pokémon Showdown login service for a local nickname.
+			this.loggingIn = name;
+			this.update(null);
+			PS.send(`/trn ${name},0,`);
+			this.loggingIn = null;
+			this.update({ success: true });
+			this.updateRegExp();
+			return;
+		}
 		this.loggingIn = name;
 		this.update(null);
 		PSLoginServer.rawQuery(
@@ -736,6 +752,10 @@ class PSUser extends PSStreamModel<PSLoginState | null> {
 		});
 	}
 	changeNameWithPassword(name: string, password: string, special: PSLoginState = { needsPassword: true }) {
+		if (Config.localAuth) {
+			this.changeName(name);
+			return;
+		}
 		this.loggingIn = name;
 		if (!password && !special) {
 			this.updateLogin({
@@ -807,9 +827,11 @@ class PSUser extends PSStreamModel<PSLoginState | null> {
 		}
 	}
 	logOut() {
-		PSLoginServer.query(
-			'logout', { userid: this.userid }
-		);
+		if (!Config.localAuth) {
+			PSLoginServer.query(
+				'logout', { userid: this.userid }
+			);
+		}
 		PS.send(`/logout`);
 		PS.connection?.disconnect();
 
diff --git a/play.pokemonshowdown.com/src/panel-battle.tsx b/play.pokemonshowdown.com/src/panel-battle.tsx
index cfd6ca1..31d8134 100644
--- a/play.pokemonshowdown.com/src/panel-battle.tsx
+++ b/play.pokemonshowdown.com/src/panel-battle.tsx
@@ -218,6 +218,12 @@ export class BattleRoom extends ChatRoom {
 	}
 
 	loadReplay() {
+		if (Config.localAuth) {
+			this.title = 'Replay unavailable';
+			this.battle.stepQueue = ['|error|Remote replay loading is disabled in local mode.'];
+			this.update(null);
+			return;
+		}
 		const replayid = this.id.slice(7);
 		Net(`https://replay.pokemonshowdown.com/${replayid}.json`).get().catch(() => '').then(data => {
 			try {
@@ -1258,15 +1264,15 @@ class BattlePanel extends PSRoomPanel<BattleRoom> {
 				<span style="float: right">
 					<a
 						onClick={this.handleDownloadReplay}
-						href={`//${Config.routes.replays}/download`}
+						href="#"
 						class="button replayDownloadButton"
 					>
 						<i class="fa fa-download" aria-hidden></i> Download replay</a>
 					<br />
 					<br />
-					<button class="button" data-cmd="/savereplay">
+					{!Config.localAuth && <button class="button" data-cmd="/savereplay">
 						<i class="fa fa-upload" aria-hidden></i> Upload and share replay
-					</button>
+					</button>}
 				</span>
 
 				<button class="button" data-cmd="/play" style="min-width:4.5em">
diff --git a/play.pokemonshowdown.com/src/panel-chat.tsx b/play.pokemonshowdown.com/src/panel-chat.tsx
index ea8cd0e..5167d9f 100644
--- a/play.pokemonshowdown.com/src/panel-chat.tsx
+++ b/play.pokemonshowdown.com/src/panel-chat.tsx
@@ -459,7 +459,7 @@ export class ChatRoom extends PSRoom {
 			}
 		},
 		'rank,ranking,rating,ladder'(target) {
-			if (PS.teams.usesLocalLadder) return `/rank ${target}`;
+			if (Config.localAuth || PS.teams.usesLocalLadder) return `/rank ${target}`;
 
 			let arg = target;
 			if (!arg) {
diff --git a/play.pokemonshowdown.com/src/panel-ladder.tsx b/play.pokemonshowdown.com/src/panel-ladder.tsx
index 7fbaba8..369cf58 100644
--- a/play.pokemonshowdown.com/src/panel-ladder.tsx
+++ b/play.pokemonshowdown.com/src/panel-ladder.tsx
@@ -47,6 +47,7 @@ export class LadderFormatRoom extends PSRoom {
 	error?: string;
 	ladderData?: LadderData;
 	ladderHTML?: string;
+	requestTimer?: ReturnType<typeof setTimeout>;
 
 	constructor(options: any) {
 		super(options);
@@ -62,6 +63,8 @@ export class LadderFormatRoom extends PSRoom {
 		this.update(null);
 	};
 	setError = (error: Error) => {
+		if (this.requestTimer) clearTimeout(this.requestTimer);
+		this.requestTimer = undefined;
 		this.loading = false;
 		this.error = error.message;
 		this.ladderData = undefined;
@@ -69,6 +72,8 @@ export class LadderFormatRoom extends PSRoom {
 		this.update(null);
 	};
 	setLadderData = (ladderData: string | undefined) => {
+		if (this.requestTimer) clearTimeout(this.requestTimer);
+		this.requestTimer = undefined;
 		this.loading = false;
 		this.error = undefined;
 		this.ladderHTML = undefined;
@@ -80,6 +85,8 @@ export class LadderFormatRoom extends PSRoom {
 		this.update(null);
 	};
 	setLadderHTML = (ladderHTML: string | undefined) => {
+		if (this.requestTimer) clearTimeout(this.requestTimer);
+		this.requestTimer = undefined;
 		this.loading = false;
 		this.error = undefined;
 		this.ladderData = undefined;
@@ -93,7 +100,11 @@ export class LadderFormatRoom extends PSRoom {
 		this.error = undefined;
 		this.ladderData = undefined;
 		this.ladderHTML = undefined;
-		if (PS.teams.usesLocalLadder) {
+		if (this.requestTimer) clearTimeout(this.requestTimer);
+		this.requestTimer = setTimeout(() => {
+			if (this.loading) this.setError(new Error('Local ladder request timed out. Please reconnect and try again.'));
+		}, 5000);
+		if (Config.localAuth || PS.teams.usesLocalLadder) {
 			const prefix = toID(this.searchValue);
 			PS.send(`/cmd laddertop ${this.format}${prefix ? ` ,${prefix}` : ''}`);
 		} else if (this.format !== undefined) {
@@ -276,10 +287,17 @@ class LadderListPanel extends PSRoomPanel {
 		if (!window.BattleFormats) {
 			return <p>Loading...</p>;
 		}
+		let formats = Object.entries(BattleFormats).filter(([, format]) => format.rated && format.searchShow);
+		if (Config.localAuth) {
+			const currentGen = Math.max(0, ...formats.map(([id]) => Number(/^gen(\d+)/.exec(id)?.[1] || 0)));
+			const currentFormats = formats.filter(([id]) => id.startsWith(`gen${currentGen}`));
+			const champions = currentFormats.filter(([id]) => id.includes('champions')).slice(0, 8);
+			const standard = currentFormats.filter(([id]) => !id.includes('champions')).slice(0, 12);
+			formats = [...standard, ...champions];
+		}
 		let currentSection = "";
 		const buf: JSX.Element[] = [];
-		for (const [id, format] of Object.entries(BattleFormats)) {
-			if (!format.rated || !format.searchShow) continue;
+		for (const [id, format] of formats) {
 			if (format.section !== currentSection) {
 				currentSection = format.section;
 				buf.push(<h3>{currentSection}</h3>);
@@ -296,11 +314,14 @@ class LadderListPanel extends PSRoomPanel {
 		const room = this.props.room;
 		return <PSPanelWrapper room={room}>
 			<div class="ladder pad">
-				<p>
+				{Config.localAuth ? <>
+					<p>本地排行榜仅显示当前世代常用格式，以免一次加载全部格式导致页面卡顿。</p>
+					<p>可在聊天中输入 <code>/rating 用户名</code> 查询本地积分。</p>
+				</> : <p>
 					<a class="button" href={`//${Config.routes.users}/`} target="_blank">
 						Look up a specific user's rating
 					</a>
-				</p>
+				</p>}
 				<p>
 					<button data-href="view-ladderhelp" class="button">
 						<i class="fa fa-info-circle" aria-hidden></i> How the ladder works
diff --git a/play.pokemonshowdown.com/src/panel-mainmenu.tsx b/play.pokemonshowdown.com/src/panel-mainmenu.tsx
index ca1196d..655b9fc 100644
--- a/play.pokemonshowdown.com/src/panel-mainmenu.tsx
+++ b/play.pokemonshowdown.com/src/panel-mainmenu.tsx
@@ -126,6 +126,10 @@ export class MainMenuRoom extends PSRoom {
 		case 'challstr': {
 			const [, challstr] = args;
 			PS.user.challstr = challstr;
+			if (Config.localAuth) {
+				PS.user.initializing = false;
+				return;
+			}
 			PSLoginServer.query(
 				'upkeep', { challstr }
 			).then(res => {
@@ -149,7 +153,7 @@ export class MainMenuRoom extends PSRoom {
 				PS.prefs.set('serversettings', { ...PS.prefs.serversettings, ...JSON.parse(settingsJSON) });
 			}
 			PS.user.setName(fullName, named, avatar);
-			PS.teams.loadRemoteTeams();
+			if (!Config.localAuth) PS.teams.loadRemoteTeams();
 			return;
 		} case 'updatechallenges': {
 			const [, challengesBuf] = args;
diff --git a/play.pokemonshowdown.com/src/panel-popups.tsx b/play.pokemonshowdown.com/src/panel-popups.tsx
index f2f29ac..ac4afdd 100644
--- a/play.pokemonshowdown.com/src/panel-popups.tsx
+++ b/play.pokemonshowdown.com/src/panel-popups.tsx
@@ -277,12 +277,12 @@ class UserPanel extends PSRoomPanel<UserRoom> {
 			) : (
 				<img src={avatar} class="trainersprite" />
 			))}
-			<strong><a
-				href={`//${Config.routes.users}/${user.userid}`} target="_blank"
-				style={`color: ${away ? '#888888' : BattleLog.usernameColor(user.userid)}`}
-			>
-				{user.name}
-			</a></strong><br />
+			<strong style={`color: ${away ? '#888888' : BattleLog.usernameColor(user.userid)}`}>
+				{Config.localAuth ? user.name : <a
+					href={`//${Config.routes.users}/${user.userid}`} target="_blank"
+					style={`color: ${away ? '#888888' : BattleLog.usernameColor(user.userid)}`}
+				>{user.name}</a>}
+			</strong><br />
 			{isSelf && !hideInteraction ? (
 				<StatusEditor />
 			) : status ? (
@@ -691,7 +691,7 @@ class OptionsPanel extends PSRoomPanel {
 				<button class="button" data-href="avatars">Avatar...</button>
 			</p>
 
-			{PS.user.named && (PS.user.registered?.userid === PS.user.userid ?
+			{!Config.localAuth && PS.user.named && (PS.user.registered?.userid === PS.user.userid ?
 				<button className="button" data-href="changepassword">Password...</button> :
 				<button className="button" data-href="register">Register</button>)}
 
@@ -1103,6 +1103,7 @@ class ChangePasswordPanel extends PSRoomPanel {
 
 	handleChangePassword = (ev: Event) => {
 		ev.preventDefault();
+		if (Config.localAuth) return this.setState({ errorMsg: "Local nickname mode does not use passwords." });
 		let oldpassword = this.base?.querySelector<HTMLInputElement>('input[name=oldpassword]')?.value;
 		let password = this.base?.querySelector<HTMLInputElement>('input[name=password]')?.value;
 		let cpassword = this.base?.querySelector<HTMLInputElement>('input[name=cpassword]')?.value;
@@ -1128,6 +1129,10 @@ class ChangePasswordPanel extends PSRoomPanel {
 
 	override render() {
 		const room = this.props.room;
+		if (Config.localAuth) return <PSPanelWrapper room={room} width={280}><div class="pad">
+			<p>Local nickname mode does not use accounts or passwords.</p>
+			<p><button type="button" data-cmd="/close" class="button">Close</button></p>
+		</div></PSPanelWrapper>;
 
 		return <PSPanelWrapper room={room} width={280}><div class="pad">
 			<form onSubmit={this.handleChangePassword}>
@@ -1182,6 +1187,7 @@ class RegisterPanel extends PSRoomPanel {
 
 	handleRegisterUser = (ev: Event) => {
 		ev.preventDefault();
+		if (Config.localAuth) return this.setState({ errorMsg: "Local nickname mode does not use account registration." });
 		let captcha = this.base?.querySelector<HTMLInputElement>('input[name=captcha]')?.value;
 		let password = this.base?.querySelector<HTMLInputElement>('input[name=password]')?.value;
 		let cpassword = this.base?.querySelector<HTMLInputElement>('input[name=cpassword]')?.value;
@@ -1214,6 +1220,10 @@ class RegisterPanel extends PSRoomPanel {
 
 	override render() {
 		const room = this.props.room;
+		if (Config.localAuth) return <PSPanelWrapper room={room} width={280}><div class="pad">
+			<p>Local nickname mode does not use account registration.</p>
+			<p><button type="button" data-cmd="/close" class="button">Close</button></p>
+		</div></PSPanelWrapper>;
 
 		return <PSPanelWrapper room={room} width={280}><div class="pad">
 			<form onSubmit={this.handleRegisterUser}>
diff --git a/play.pokemonshowdown.com/src/panel-teambuilder-team.tsx b/play.pokemonshowdown.com/src/panel-teambuilder-team.tsx
index b14cf76..9ede698 100644
--- a/play.pokemonshowdown.com/src/panel-teambuilder-team.tsx
+++ b/play.pokemonshowdown.com/src/panel-teambuilder-team.tsx
@@ -5,7 +5,7 @@
  * @license AGPLv3
  */
 
-import { PS, PSRoom, type RoomOptions, type Team } from "./client-main";
+import { Config, PS, PSRoom, type RoomOptions, type Team } from "./client-main";
 import { PSPanelWrapper, PSRoomPanel } from "./panels";
 import { toID, type ID } from "./battle-dex";
 import { BattleLog } from "./battle-log";
@@ -118,6 +118,7 @@ class TeamPanel extends PSRoomPanel<TeamRoom> {
 	static formatResources = {} as Record<string, FormatResource>;
 
 	static getFormatResources(format: string): Promise<FormatResource> {
+		if (Config.localAuth) return Promise.resolve(null);
 		if (format in this.formatResources) return Promise.resolve(this.formatResources[format]);
 		return Net('https://www.smogon.com/dex/api/formats/by-ps-name/' + format).get()
 			.then(result => {
diff --git a/play.pokemonshowdown.com/testclient-new.html b/play.pokemonshowdown.com/testclient-new.html
index d36feae..84992af 100644
--- a/play.pokemonshowdown.com/testclient-new.html
+++ b/play.pokemonshowdown.com/testclient-new.html
@@ -103,6 +103,7 @@
 	<script nomodule src="/js/lib/ps-polyfill.js"></script>
 	<script src="../config/testclient-key.js"></script>
 	<script src="js/client-core.js" onerror="alert('You must build the client with `node build` before using testclient.html')"></script>
+	<script src="js/localization-zh-hans.js"></script>
 
 	<script src="js/battle-dex-data.js"></script>
 	<script src="js/battle-dex.js"></script>
@@ -114,6 +115,7 @@
 	<script> Net.defaultRoute = 'https://play.pokemonshowdown.com'; </script>
 
 	<script src="js/lib/preact.min.js"></script>
+	<script src="js/localization-preact-init.js"></script>
 	<script src="js/panels.js"></script>
 	<script src="js/panel-mainmenu.js"></script>
 	<script src="js/panel-rooms.js"></script>
